import { ParticlesDirective } from './particles.directive';

describe('ParticlesDirective', () => {
  it('should create an instance', () => {
    const directive = new ParticlesDirective();
    expect(directive).toBeTruthy();
  });
});
